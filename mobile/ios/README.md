# iOS host

Generate the pinned Flutter iOS host with Flutter 3.35.1 on macOS before adding the narrow Swift integrations. iOS does not support arbitrary cross-app screen inspection or a system-wide overlay.
